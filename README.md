# My Full Stack Virtual AI Accelerator
A project about out-of-tree kernel module, yocto custom layer / image, MLIR runtime all-in-one.

## Environment Setup
ArchLinux(developing) --> Ubuntu docker(building yocto)--> QEMU x86-64(validate kernel modules/ hw simulations)
We will be using ubuntu docker to do the work, the reason why is that I dont wanna pollute my environment ofc, secondly and the most important one: package management; instead of installing one-by-one by pacman, some are just in AUR, why not just use ubuntu as yocto project is expected.

## Overview
### Virtual Hardware Simulation

### Linux Kernel Driver

### Yocto Integration

### MLIR Runtime

